'use client';

export function DemoBanner() {
  return (
    <div className="cp-demo-banner">
      DEMO — Fictional data only (Alex Rivera). Not a live medical product. Reset anytime from Parent → Settings.
    </div>
  );
}
